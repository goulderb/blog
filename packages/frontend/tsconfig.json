{
  "extends": "@tsconfig/node14/tsconfig.json",
  // Exclude node modules and dist folders.
  "exclude": ["**/node_modules", "**/dist", "**/*.spec.ts", "**/*.test.ts"],
  "compilerOptions": {
    // Always emit type declaration files
    "declaration": true
  }
}
